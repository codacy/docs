The updated settings will be used on the next analysis, but you can click  
**reanalyze the latest commit of your branches now** on the notification that
appears at the bottom of the page to trigger an analysis immediately.

import AnalyzeNow from '@site/docs/repositories-configure/images/file-extensions.png';

<img src={AnalyzeNow} alt="Analyze now" />
