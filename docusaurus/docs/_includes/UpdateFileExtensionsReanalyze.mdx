export default function UpdateFileExtensionsReanalyze() {
  return (
    <>
      <p>
        The updated settings will be used on the next analysis, but you can
        click <strong>reanalyze the latest commit of your branches now</strong>{' '}
        on the notification that appears at the bottom of the page to trigger an
        analysis immediately.
      </p>

      <img src="/img/file-extensions-analyze.png" alt="Analyze now" />
    </>
  );
}
