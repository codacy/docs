StylesPath = .github/styles
MinAlertLevel = warning
SkippedScopes = style
IgnoredScopes = code, strong, tt
IgnoredClasses = skip-vale
Vocab = Codacy

[*.md]
# Ignore Markdown includes and bare URLs
TokenIgnores = ({%\s*include.*%})|(<(?!!--).*>)|({:.*})|({{.*}})
BlockIgnores = (?s){%\s*include.*%}

BasedOnStyles = Vale, Microsoft, alex

Microsoft.Avoid = NO
Microsoft.FirstPerson = suggestion
Microsoft.GeneralURL = NO
Microsoft.HeadingAcronyms = NO
Microsoft.HeadingPunctuation = .
Microsoft.HeadingColons = NO
Microsoft.We = NO
# These rules check front matter values as if they were prose (Vale doesn't
# exempt YAML front matter), so any hyphenated numbers there -- like ISO dates
# (2026-01-16) -- get flagged as "ranges needing an en dash". Already noisy on
# existing prose too (271 hits repo-wide before this was ever enforced).
Microsoft.RangeFormat = NO
Microsoft.Ranges = NO

alex.Profanity = suggestion
alex.Ablist = suggestion
