{
    "ignorePatterns": [
        {
            "pattern": "^(?!http).+\\.md(#.+)?$"
        },
        {
            "pattern": "^images\\"
        }
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "https://docs.codacy.com/"
        }
    ]
}